organization: # Name of your organization (mandatory)
  name: "Deel Implementation"

database:         # External database configuration
  host: 'postgres'
  port: 5432
  name: 'conduktor'
  username: 'conduktor'
  password: 'conduktor'
  connection_timeout: 30 # in seconds

admin:            # Local admin credentials
  email: "admin@test.com"
  password: "admin"

clusters:
  - id: my-local-kafka-cluster
    name: My Deel Local Kafka Cluster
    bootstrapServers: "kafka:9092"
    